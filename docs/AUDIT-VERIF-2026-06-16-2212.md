# Audit de vérification — HaloCard — 2026-06-16 22:12

> Mandat : prospection terrain mi-juillet 2026. Cet audit confirme (ou non) que
> la base est saine pour démontrer le produit à Genève. **Read-only** : aucune
> écriture prod, aucune migration, aucun fichier source modifié (hors ce rapport).
> Périmètre vérifié : repo `~/Projects/Carte-Fidelite` @ `main` (HEAD `b2613e2`),
> Supabase `WalletCard` (`oqcelbbozpykwkasjtqy`), Vercel `carte-fidelite`
> (`prj_D1DFdqrxrtBrFjbl8xwPUBj9fCbl`), domaines halocard.ch / app.halocard.ch.

---

## 🟢 Verdict Go/No-Go

- **Santé globale : SAINE.**
- **~58 PASS / 6 PARTIEL / 0 FAIL bloquant.** (Aucun FAIL critique ; aucune fuite
  cross-tenant ; aucun secret en clair ; 6 invariants respectés.)
- **Go / No-Go pour la démo terrain : OUI.** Le produit est démontrable en l'état
  (parcours public `/c/boulangerie-demo` vivant, carte Apple/Google générée
  correctement, F1/F2/F3 fonctionnels, prod en ligne, 0 erreur fatale runtime 48 h).
- **Aucun bug latent critique découvert.** Les 6 PARTIEL sont des dettes
  non-bloquantes (1 vuln high dans l'outillage de test, CSP en report-only,
  builds preview en échec, 1 orphelin d'audit prod, carte démo sans visuel
  publié, snapshot billing vide). Détail bloc par bloc + plan d'action ci-dessous.

**3 actions prioritaires (avant le terrain) :**
1. **Polir la carte de démo** : merger #33 (rendu des tampons) **et** publier un
   `card_design` pour Boulangerie Démo — aujourd'hui `logo_url=null` + aucun design
   publié ⇒ la carte n'affiche **ni photo ni strip de tampons** (elle paraît figée).
2. **Roter le mot de passe de l'admin démo** avant la prospection (item de sécu
   reporté, non re-vérifié ici — à confirmer côté Supabase Auth).
3. **Nettoyer la dette technique légère** : `npm audit fix` (supprime l'unique
   vuln *high* `vite`) + formaliser l'orphelin `MARKETING_CONSENT_UPDATED`.

---

## Bloc par bloc

### BLOC 1 — Environnement & build

| Critère | Statut | Preuve | Action |
|---|---|---|---|
| 1.1 node ≥ 22 / pnpm ≥ 10 | 🟡 PARTIEL | `.nvmrc`=`22` ✅. node local `v24.15.0`. **Le repo est npm, pas pnpm** : `package-lock.json` présent, pas de `pnpm-lock.yaml`, `package.json` sans `packageManager`. CLAUDE.md L37 dit `npx vitest run`. J'ai exécuté l'outillage directement (comme la CI). | Lever l'ambiguïté pnpm/npm dans le mandat ; aligner CLAUDE.md/CI |
| 1.2 install propre | ✅ PASS | `node_modules` présent ; build log Vercel « up to date in 1s » sans erreur d'intégrité | — |
| 1.3 `tsc --noEmit` 0 err | ✅ PASS | `npx tsc --noEmit` → exit 0, aucune sortie | — |
| 1.4 lint 0 err | ✅ PASS | `npx eslint .` → exit 0 | — |
| 1.5 vitest tous verts | ✅ PASS | `npx vitest run` → **Test Files 107 passed (107) / Tests 753 passed (753)**, 7.13 s | — |
| 1.5b ≥ 762 tests | 🟡 PARTIEL | **753 sur `main`** ; le « 762 » d'hier est le compte de la **branche #33** (non mergée, +9 tests `stampStrip`). Pas une régression. | Informationnel |
| 1.6 `audit --level=high` = 0 high | 🟡 PARTIEL | `npm audit` → **10 vulns (1 low, 8 moderate, 1 high)**. Le *high* = `vite` (GHSA-v6wh-96g9-6wx3 / fx2h-pf6j-xcff, Windows-only) tiré par **vitest** = chaîne de **test**, hors runtime prod déployé. Fix non-breaking dispo. | `npm audit fix` |
| 1.7 Next verrouillé | ✅ PASS | `package.json` `"next":"^16.2.9"` ; build Vercel « Detected Next.js version: 16.2.9 » ; AGENTS.md respecté | — |

### BLOC 2 — Les 6 invariants

| Inv. | Statut | Preuve |
|---|---|---|
| 2.1 INV.1 AUDIT_ACTIONS ⊂ CHECK prod | ✅ PASS | `auditActionsSync.test.ts` vert (dans les 753). **Set math** : code (47 actions, `auditLog.ts:7-66`) ⊂ CHECK prod (51) → **0 écart**. `MARKETING_CONSENT_UPDATED` = **seul** orphelin prod (présent dans le CHECK live, absent du repo) — confirmé, cf. Annexe B. |
| 2.2 INV.2 Google jamais UPDATE/PUT | ✅ PASS | `wallet/googleClass.ts:63-68` : `get` → `patch` ; `insert` seulement sur 404. Aucun `.update(`/PUT vers l'API loyalty. `googlePass.ts:86-88` documente l'invariant. (Les `.update(` trouvés visent la table Supabase `loyalty_cards`, pas Google.) |
| 2.3 INV.3 Tenancy | ✅ PASS | **45 fichiers de route** importent `supabaseAdmin` ; **0 RISK**. Chacun : filtre `merchant_id`/`id`-par-tenant, OU `currentMerchantId()`/`currentMerchantContext()`, OU `requireAdminApi`, OU token wallet/`CRON_SECRET`. Matrice complète Annexe C. |
| 2.4 INV.4 scan atomique | ✅ PASS | `api/scan/route.ts:19` `supabaseAdmin.rpc("scan_increment", …)` ; `:22-28` **fail-loud** (`throw` si erreur/vide). Aucun read-modify-write sur `stamps_count` (la RPC écrit, `route.ts:111`). |
| 2.5 INV.5 aucun secret en clair | ✅ PASS | `.gitignore` : `.env*` (+`!.env.example`), `*.pem`, `certs/`. Scan strict `src/` (`sk_`, `re_…`, `eyJ`, `BEGIN PRIVATE KEY`) → **0 hit**. Seul littéral `ChIJ` = fixture démo `lib/demo/constants.ts:16` (`…ReXAMPLEdemo00`). Credentials lus depuis env/`certs/`. |
| 2.6 INV.6 migrations alignées | 🟡 PARTIEL | `schema_migrations` prod = 45 entrées. **Orphelins prod sans fichier repo** : `marketing_consent` (20260611223639) + `audit_actions_marketing_consent` (20260611223654) → source de l'orphelin `MARKETING_CONSENT_UPDATED` ; + doublon `audit_actions_card_design` (20260609/20260610) ; + `campaigns_self_service_4b`. Les `2024xx_*` (init) ne sont pas trackées (appliquées avant tracking). **Aucun impact runtime** (le code n'émet que les 47 actions valides). |

### BLOC 3 — Garde anti-drift schéma ↔ code

| Critère | Statut | Preuve |
|---|---|---|
| 3.1 `merchantsColumnsSync.test.ts` vert | ✅ PASS | Existe (`src/lib/__tests__/`), vert. Parse migrations (`CREATE`/`ALTER … ADD COLUMN`) ∩ `.from("merchants").select(...)` ; assert `read ⊆ defined`. |
| 3.2 merchants : colonnes lues ∈ prod | ✅ PASS | 41 colonnes lues, **toutes présentes** en prod. Annexe A. |
| 3.3 customers/loyalty_cards/scan_history/campaigns/audit_logs | ✅ PASS | Toutes les colonnes lues existent en prod. **Faux-positif levé** : `push_token` est lu sur `wallet_device_registrations` (`wallet/channel.ts:20`), **pas** sur `loyalty_cards`. Annexe A. ⚠️ Garde ColumnsSync **uniquement** sur `merchants` (pas les 5 autres). |

### BLOC 4 — F1 « Carte vivante »

| Critère | Statut | Preuve |
|---|---|---|
| 4.1 Colonnes prod merchants | ✅ PASS | `phone`(text), `reward_label`(text), `business_hours`(jsonb), `address`(text), `logo_url`(text), `latitude`/`longitude`(double) tous présents. |
| 4.2 Boulangerie Démo renseignée | 🟡 PARTIEL | `phone="+41 22 555 00 00"`, `primary_color="#C8862E"`, `address="Rue du Marché 12, 1204 Genève"`, `business_hours`≠null, `reward_label="Un café offert"`, `google_place_id="ChIJ…demo00"` ✅ — **mais `logo_url=null`**. |
| 4.3 `identityFromMerchant.ts` | ✅ PASS | `identityFromMerchant.ts:41-58` injecte reward/address/phone/hours/maps/review dans passJson (Apple) + googleIdentity (Google). |
| 4.4 Génération pass locale (RÉELLE) | ✅ PASS | **Run live** des fonctions pures du repo. Apple `backFields` = review*+hours+address+maps+phone ; `storeCard.secondaryFields` contient la récompense ; address/phone = strings (PKDataDetector tappables). Google `textModulesData`(reward,hours)+`linksModuleData`(maps,phone). Annexes E/F. |
| 4.5 strip si design publié | 🟡 PARTIEL | **Aucun `card_design` publié pour Boulangerie Démo** (table `card_designs` : pas de ligne pour ce merchant) + `logo_url=null` ⇒ **strip/heroImage ABSENT** (confirmé dans le payload). Cohérent avec #33 (rendu strip encore en draft). Pour la démo : carte sans visuel de tampons. |

### BLOC 5 — F2 « Lien avis Google »

| Critère | Statut | Preuve |
|---|---|---|
| 5.1 CHECK `{10,255}` | ✅ PASS | `pg_get_constraintdef` : `CHECK ((google_place_id IS NULL) OR (google_place_id ~ '^ChIJ[A-Za-z0-9_-]{10,255}$'))` — **255**, bug latent corrigé (PR #32). |
| 5.2 JS `PLACE_ID_RE {10,255}` | ✅ PASS | `wallet/googleReview.ts:14` aligné `{10,255}` ; `isValidPlaceId`/`reviewUrlFor`/`shouldShowReview` testés (dans les 753). |
| 5.3 Logique de présence | ✅ PASS | `identityFromMerchant.ts:54` : `reviewUrl` gaté par `shouldShowReview(rewardReady, google_place_id)`. |
| 5.4 Bascule 9 → 10 tampons | ✅ PASS | **Run live** : état 9 → `reviewUrl=null` (pas de backField/linksModule review) ; état 10 → backField Apple « ★ LAISSER UN AVIS GOOGLE » + `linksModuleData` Google en tête. Bascule **absent → présent** prouvée. Annexes E/F. |

### BLOC 6 — F3 « Bienvenue + récompense intermédiaire »

| Critère | Statut | Preuve |
|---|---|---|
| 6.1 Types | ✅ PASS | `loyalty/types.ts:6` `welcome_stamps?:0\|1` ; `:8` `intermediate_milestone?:number\|null` ; `:21` ScanEvent `intermediate_reward_ready`. |
| 6.2 `initialStampsForEnroll` | ✅ PASS | `engine.ts:62-66` `welcome_stamps===1 ? Math.min(1,goal) : 0` ; run live → **1** pour la démo. |
| 6.3 intermediate jamais avec reward_ready | ✅ PASS | `engine.ts:23-27` : `reward_ready` (count==goal) et `intermediate_reward_ready` (milestone) mutuellement exclusifs ; validation garantit `intermediate < goal`. |
| 6.4 `demoParcours.test.ts` sans `it.todo` | ✅ PASS | `grep -nE 'it\.todo\|test\.todo'` → **aucun**. Couvre les 3 mécaniques + welcome + intermediate + reward_ready + bordures. |

### BLOC 7 — Tenancy & RLS (Supabase)

| Critère | Statut | Preuve |
|---|---|---|
| 7.1 RLS + ≥1 policy sur tables sensibles | ✅ PASS | RLS activé partout. Policies : merchants 4, card_designs 2, les autres 1. **`campaign_sends` : RLS ON, 0 policy** (deny-all par défaut — pas d'accès anon/auth ; toujours via `supabaseAdmin` scopé) → exception documentée, **sûre**. |
| 7.2 Advisors security — aucune NOUVELLE alerte | ✅ PASS | 4 lints : `campaign_sends` no-policy (INFO), `is_admin` SECURITY DEFINER exécutable anon+auth (2×WARN), `leaked_password_protection` off (WARN). **Identiques à hier** = dettes connues, **non bloquantes**. Annexe D. |
| 7.3 Advisors performance — aucune régression | ✅ PASS | **13** `auth_rls_initplan` (WARN), 10 `unused_index` (INFO), `multiple_permissive_policies` sur `card_designs` (WARN). (Mandat citait 14 initplan ; 13 mesurés — pas de régression.) Dette perf documentée. Annexe D. |

### BLOC 8 — Outil compte démo (#29)

| Critère | Statut | Preuve |
|---|---|---|
| 8.1 seed gardé | ✅ PASS | `demo/seed/route.ts:14-15` `requireAdminApi` **en premier** ; `:20` rate-limit `demo-seed:${userId}` 10/h ; `:31-37` audit `DEMO_ACCOUNT_SEEDED` `details:{mode,slug}` ; `lib/demo/seed.ts:88-92` `email_confirm:true`. |
| 8.2 reset triple-garde | ✅ PASS | `demo/reset.ts:22` `assertDemoMerchant` **avant** `purgeDemoCustomerData` ; `demo/identity.ts:35-46` triple garde (slug réservé + email `@example.com` + role). Tous les DELETE (`purge.ts`) scopés au `merchantId` validé. Aucun chemin de delete sans garde. |
| 8.3 BD démo : état | 🟡 PARTIEL | `phone`/`primary_color`/`address`/`business_hours`/`reward_label`/`google_place_id` posés ✅ ; **`logo_url=null`** et **aucun `card_design` publié**. |
| 8.4 Aucun compte Auth orphelin | ✅ PASS | `auth.users` `boulangerie-demo@example.com` → `merchant_rows=1` (pas d'orphelin). |
| 8.5 Smoke `/c/boulangerie-demo` | ✅ PASS | `HTTP/2 200` ; titre « Carte de fidélité Boulangerie Démo » ; page contient « Apple Wallet » + « Google Wallet ». |

### BLOC 9 — Observabilité (Sentry)

| Critère | Statut | Preuve |
|---|---|---|
| 9.1 `onRequestError` | ✅ PASS | `instrumentation.ts:14` `export const onRequestError = Sentry.captureRequestError;` |
| 9.2 sentryOptions + configs | ✅ PASS | `sentryOptions.ts:16` `enabled:Boolean(SENTRY_DSN)` ; `:18` `beforeSend:scrubBeforeSend` (PII) ; `:17` `tracesSampleRate:0.1`. `sentry.server.config.ts`/`sentry.edge.config.ts`/`instrumentation-client.ts` présents. |
| 9.3 DSN en prod, pas « Sensitive » | 🟡 PARTIEL | **Non énumérable** sans CLI Vercel authentifié (hors périmètre sandbox). Preuves indirectes : builds **production** READY (donc DSN non bloquant), CSP `connect-src` inclut `*.sentry.io`/`*.ingest.sentry.io`, Sentry prouvé fonctionnel hier. | Vérifier manuellement la flag « Sensitive » côté Vercel |
| 9.4 Logs runtime 24 h | ✅ PASS | `get_runtime_logs` (error+fatal, 48 h, production) → **« No logs found »** = 0 erreur fatale, pas de 5xx en boucle. |

### BLOC 10 — Email (Resend)

| Critère | Statut | Preuve |
|---|---|---|
| 10.1 `RESEND_API_KEY`+`EMAIL_FROM` en prod | 🟡 PARTIEL | Non énumérable (idem 9.3). CLAUDE.md L10 + audit d'hier les déclarent posés ; le code no-op proprement si absents. | Confirmer côté Vercel |
| 10.2 `isEmailConfigured` + chaîne fetch | ✅ PASS | `email/send.ts:28-30` `Boolean(RESEND_API_KEY && EMAIL_FROM)` ; `:41-55` POST `https://api.resend.com/emails` (Bearer). Tests verts. |
| 10.3 Trace d'envoi récent | 🟡 PARTIEL | Pas de table de logs d'email ; `leads`=9, audit `DEMO_ACCOUNT_SEEDED`×2 (seed envoie un mot de passe temporaire). Pas de preuve directe d'un email Resend délivré (no-op silencieux si clé absente). | Envoyer un email test depuis l'admin avant le terrain |

### BLOC 11 — Cron & background

| Critère | Statut | Preuve |
|---|---|---|
| 11.1 routes cron gardées | ✅ PASS | `cron/billing-snapshot/route.ts:52-56` `Bearer ${CRON_SECRET}` via `timingSafeEqualStr` (fail-closed si secret absent) ; `cron/campaigns/route.ts:20-21` idem. |
| 11.2 `vercel.json` crons | ✅ PASS | `crons`: `/api/cron/campaigns` `0 9 * * *` (quotidien) ; `/api/cron/billing-snapshot` `0 5 1 * *` (**mensuel, 1er**). |
| 11.3 `billing_snapshots` ≥ 1 ligne | 🟡 PARTIEL | **0 ligne**. Justifié : feature billing posée le 2026-06-10, le cron mensuel (1er du mois) n'a pas encore eu de frontière de mois depuis ; `cron_runs`=6 (les crons s'exécutent). | Informationnel |

### BLOC 12 — PR en vol (#33 Studio)

| Critère | Statut | Preuve |
|---|---|---|
| 12.1 État branche | ✅ PASS | `feat/studio-rules-stamp-render` @ `77f4554` (flakoltf, 2026-06-16), **1 commit** devant `origin/main`. |
| 12.2 Incrément | ✅ PASS | `git diff --stat` : `applePass.ts` +36, `cardDesign/stampStrip.ts` +109 (nouveau, générateur SVG **pur**), `stampStrip.test.ts` +77 (9 tests). Strip généré **côté serveur** dans `buildApplePassBuffer`, **fail-open** (try/catch → pas de strip, pass valide). |
| 12.3 vs décision « composite, B avant C » | 🟡 PARTIEL | **Priorité A seule** implémentée. C'est une **précédence** (un visuel uploadé gagne le slot `strip.png`, sinon strip de tampons généré) — **pas** un composite photo+tampons superposés. Composite **pas encore** fait ; B/C en PR suivantes (cf. message de commit). |
| 12.4 INV.2 non violé | ✅ PASS | Diff Apple-only (`applePass.ts`/`stampStrip.ts`) ; aucun `loyaltyclass.update`/PUT Google réintroduit. |
| 12.5 Tests / régression | ✅ PASS | +9 tests purs (`stampStrip`), commit revendique 762 verts/tsc 0/lint 0 sur la branche. Aucune régression sur `main` (753 verts). |

### BLOC 13 — Détresses connues (vs audit d'hier)

| # | Évolué ? | Preuve |
|---|---|---|
| 13.1 `MARKETING_CONSENT_UPDATED` prod sans repo | **Non** | Toujours dans le CHECK live ; toujours absent du repo (`AUDIT_ACTIONS` + dernière migration). Seul orphelin. |
| 13.2 Builds preview Vercel échouent sur toutes les PR | **Non** | `list_deployments` : **tous** les previews (PR #29-#34) en `state:ERROR` ; les déploiements `target:production` (sur `main`) en `READY`. Build log #33 : warnings Turbopack « unexpected file in NFT list » (`next.config.ts`) puis échec. Non bloquant (CI GitHub = juge ; prod OK). |
| 13.3 Tampons non rendus sur le vrai pass | **Oui (en cours)** | #33 livre le rendu strip (formes vectorielles) — non mergé. |
| 13.4 F4/F5 | **Non** | Toujours reportés sprint août (aucun code). |
| 13.5 Publishing access Google Wallet | **Non** | Toujours côté Google ; bouton gaté `NEXT_PUBLIC_GOOGLE_WALLET_READY`. |

### BLOC 14 — Smokes publics (read-only)

| Critère | Statut | Preuve |
|---|---|---|
| 14.1 `halocard.ch` | ✅ PASS | `HTTP/2 200`, 351 KB, `<title>…HaloCard Genève</title>` (marketing présent). |
| 14.2 `app.halocard.ch/login` | ✅ PASS | `HTTP/2 200`, page de login (« HALO — Cartes de fidélité… »). |
| 14.3 `app.halocard.ch/c/boulangerie-demo` | ✅ PASS | `HTTP/2 200`, nom commerce + invite Apple/Google Wallet. |
| 14.4 Headers sécurité | 🟡 PARTIEL | `strict-transport-security: max-age=31536000; includeSubDomains` ✅ ; `x-content-type-options: nosniff` ✅ ; `x-frame-options: DENY` ✅ ; **CSP = `content-security-policy-report-only`** (n'applique pas, ne bloque pas). | Passer la CSP en mode enforcing après vérif des rapports |
| 14.5 robots / sitemap | ✅ PASS | `robots.txt` 200 (Disallow `/api/`,`/dashboard`,`/admin`,`/scan`,`/login`,`/signup`) ; `sitemap.xml` 200. |

---

## Action plan priorisé

| ID | Gravité | Effort | Bénéfice | Action |
|---|---|---|---|---|
| A1 | 🟠 | M | Démo convaincante | Merger #33 **et** publier un `card_design` (type tampons) pour Boulangerie Démo + poser `logo_url` → la carte affiche le strip qui se remplit (sinon carte « figée » au comptoir). |
| A2 | 🟠 | S | Sécurité terrain | Roter le mot de passe de l'admin démo avant la prospection (item reporté ; confirmer côté Supabase Auth). |
| A3 | 🟡 | S | Hygiène | `npm audit fix` → supprime l'unique vuln *high* (`vite`, chaîne de test). |
| A4 | 🟡 | S | Fin du drift | Formaliser `MARKETING_CONSENT_UPDATED` : l'ajouter à `AUDIT_ACTIONS` + committer dans le repo la migration `marketing_consent` appliquée en prod (sinon une réapplication des migrations sur un env neuf retirerait l'action). |
| A5 | 🟡 | M | Prévisualiser les PR | Réparer les builds preview Vercel (warning Turbopack NFT sur `next.config.ts`) pour pouvoir prévisualiser #33 avant merge. |
| A6 | 🟡 | S | Durcissement | Passer la CSP de `report-only` à enforcing après revue des rapports. |
| A7 | 🔵 | S | Confiance prod | Confirmer côté Vercel la présence (non « Sensitive » pour le DSN public) de `NEXT_PUBLIC_SENTRY_DSN`, `RESEND_API_KEY`, `EMAIL_FROM` ; envoyer 1 email test. |
| A8 | 🔵 | M | Anti-drift | Étendre une garde type `ColumnsSync` à customers/loyalty_cards/scan_history/campaigns/audit_logs (aujourd'hui seul `merchants` est gardé). |

---

## Annexes techniques

### A. Matrice colonnes BD ↔ code (tables sensibles)

Toutes les colonnes **lues par le code** existent en prod (vérifié `information_schema.columns`). Aucun drift.

| Table | Colonnes lues par `src/` | Statut |
|---|---|---|
| merchants | 41 col. (id, user_id, role, shop_name, email, slug, primary_color, logo_url, address, phone, latitude, longitude, business_hours, google_place_id, reward_label, stamp_goal, loyalty_type, loyalty_config, segment_config, business_type, enrollment_token, suspended_at/_reason, plan, plan_started_at, plan_cap_override, billing_cycle/_status/_provider, launch_partner, trial_ends_at, managed_by_concierge, concierge_design_*, onboarding_*, setup_mode, signup_source, created_at, dashboard_config) | ✅ toutes en prod |
| customers | id, merchant_id, full_name, email, phone, created_at | ✅ (prod a en + `marketing_consent*`, non lues) |
| loyalty_cards | id, merchant_id, customer_id, stamps_count, pass_type, auth_token, pass_message, pass_updated_at, external_id, last_scan, created_at | ✅ (`push_token` ≠ cette table → `wallet_device_registrations`) |
| scan_history | id, merchant_id, card_id, points_added, scanned_at | ✅ |
| campaigns | id, merchant_id, audience, title, body, mode, run_on, active, cooldown_days, last_run_on | ✅ |
| audit_logs | id, merchant_id, user_id, card_id, action, details, ip_address, user_agent, created_at | ✅ |

### B. AUDIT_ACTIONS repo ↔ prod

- **Repo `AUDIT_ACTIONS`** (`auditLog.ts:7-66`) : **47** actions.
- **CHECK prod** `audit_logs_action_check` : **51** actions.
- **code ⊂ prod** : ✅ 0 écart (INV.1 OK).
- **Orphelins prod (dans CHECK, hors repo)** : `MARKETING_CONSENT_UPDATED` (×1 — confirmé). `SUBSCRIPTION_CANCELED`/`PAYMENT_SUCCEEDED`/`PAYMENT_FAILED` sont dans la **dernière migration repo** mais pas dans le tableau TS (réservées billing — sans risque, le test vérifie code ⊂ migration).

### C. Routes API `supabaseAdmin` → tenant filtré

**45 routes**, **0 RISK**. Mécanismes : `requireAdminApi` (toutes les routes `admin/*`), `currentMerchantId()`/`currentMerchantContext()` (merchant/onboarding/generate-*), `.eq("merchant_id"/"id"…)`, token wallet `auth_token` (routes `wallet/apple/*`), `CRON_SECRET` (cron). Routes publiques (`enroll`, `enroll/[cardId]`, `signup`) scopées par slug/token/auth-provisioning.

**2 zones « defense-in-depth » (non-RISK, à surveiller)** :
- `customers/[id]` DELETE/PATCH : mutation `.eq("id", id)` **précédée** d'un check `customer.merchant_id !== merchantId` → 403 (`route.ts:68,139`). Sûr, mais le SQL final n'est pas lui-même tenant-scopé (cf. `redeem/route.ts:54-55` qui ajoute `.eq("merchant_id")` — modèle plus défensif).
- `enroll/[cardId]` : update pass-type `.eq("id")` après la garde slug/token amont.

### D. Advisors Supabase

**Security** (inchangés vs hier — dettes connues) :
| Lint | Niveau | Objet |
|---|---|---|
| rls_enabled_no_policy | INFO | `campaign_sends` (deny-all assumé) |
| anon_security_definer_function_executable | WARN | `is_admin()` exécutable par `anon` |
| authenticated_security_definer_function_executable | WARN | `is_admin()` exécutable par `authenticated` |
| auth_leaked_password_protection | WARN | HIBP désactivé |

**Performance** (dette, pas de régression) :
| Lint | Niveau | Compte |
|---|---|---|
| auth_rls_initplan | WARN | 13 policies (`(select auth.*)` recommandé) |
| unused_index | INFO | 10 index |
| multiple_permissive_policies | WARN | `card_designs` SELECT (×5 rôles) |

### E. Payload Apple Pass — démo (extrait, run live, tronqué)

État A (stamps=9, reward NON prêt) — `backFields` (pas de `review`) :
```json
"storeCard": { "secondaryFields": [ {"key":"reward","label":"RÉCOMPENSE","value":"Un café offert"} ] },
"backFields": [
  {"key":"hours","label":"AUJOURD'HUI","value":"07:00 – 19:00"},
  {"key":"address","value":"Rue du Marché 12, 1204 Genève"},
  {"key":"maps","value":"https://www.google.com/maps/search/?api=1&query=46.2044,6.1432"},
  {"key":"phone","value":"+41 22 555 00 00"}
]
// top-level: PAS de clé "strip"/image (logo_url null, aucun design publié)
```
État B (stamps=10, reward prêt) — `backFields` gagne en tête :
```json
{"key":"review","label":"★ LAISSER UN AVIS GOOGLE",
 "value":"https://search.google.com/local/writereview?placeid=ChIJ51bQ7E5kjEcReXAMPLEdemo00"}
```

### F. Payload Google équivalent (extrait, run live)

```json
"textModulesData": [
  {"id":"reward","header":"Récompense","body":"Un café offert"},
  {"id":"hours","header":"Aujourd'hui","body":"07:00 – 19:00"}
],
"linksModuleData": { "uris": [
  // état B uniquement : {"uri":"https://search.google.com/local/writereview?placeid=ChIJ…demo00","description":"★ Laisser un avis Google","id":"review"},
  {"uri":"https://www.google.com/maps/search/?api=1&query=46.2044,6.1432","id":"maps"},
  {"uri":"tel:+41225550000","id":"phone"}
] }
// pas de heroImage (aucun design publié)
```

---

*Audit produit en read-only. Aucun fichier source modifié, aucune écriture prod,
aucune migration. Seul ce rapport est ajouté. — Claude (Principal Eng. + AppSec).*
